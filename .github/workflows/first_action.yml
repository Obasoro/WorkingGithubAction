name: My first Github action

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4
        - name: Welcome to first action
          run: echo " My first Github action"

        - name: list files
          run: ls

        - name: Read files
          run: cat README.md